---
title: SafeSpace Network
description: Guía del track SafeSpace Network del AWSpectrum Impact Lab.
template: splash
editUrl: false
pagefind: false
head:
  # Sin esto Starlight compone "SafeSpace Network | SafeSpace Network",
  # porque el título de la página coincide con el del sitio.
  - tag: title
    content: SafeSpace Network · AWSpectrum Impact Lab
---

import Hero from "../../components/Hero.astro";
import Stats from "../../components/Stats.astro";
import TrackCard from "../../components/TrackCard.astro";
import Flow from "../../components/Flow.astro";
import Phases from "../../components/Phases.astro";
import Note from "../../components/Note.astro";
import Reveal from "../../components/Reveal.astro";
import { announcedTracks } from "../../config/tracks";
import { EVENT, ROLES } from "../../config/event";

<Hero />

<Stats />

<section class="section" data-reveal>
  <p class="section__kicker">01 · Los tracks</p>
  <h2 class="section__title">Elige tu track</h2>
  <p class="section__lead">
    Ninguno es un ejemplo de juguete: los dos son aplicaciones con frontend,
    API y base de datos, desplegadas en tu propia cuenta de AWS con
    infraestructura como código. Se construyen con los mismos servicios, así
    que lo que aprendas sirve para los dos. Cualquier persona puede elegir
    cualquiera.
  </p>

  <Note kind="community" title="El mapa no es toda la historia">
    En SafeSpace Network el recurso es la entidad principal. Si una dirección
    está publicada por una fuente directa y su exposición es apropiada, aparece
    también en el mapa; si el recurso funciona por teléfono, chat o canalización,
    permanece como ficha de contacto.
  </Note>

  <div class="tracks not-content">
    {announcedTracks().map((track) => <TrackCard track={track} />)}
  </div>

  <Flow />
</section>

<section class="section" data-reveal>
  <p class="section__kicker">02 · El día</p>
  <h2 class="section__title">Cómo se reparten las tres horas</h2>
  <p class="section__lead">
    El bloque de construcción se lleva casi la mitad del tiempo. El resto es
    contexto, integración y contar lo que hiciste —que también es parte del
    trabajo.
  </p>

  <Phases />
</section>

<section class="section" data-reveal>
  <p class="section__kicker">03 · Tu equipo</p>
  <h2 class="section__title">Cinco roles, un repositorio</h2>
  <p class="section__lead">
    Dentro del track te integras a un equipo pequeño y eliges un rol. Se trabaja
    como se trabaja de verdad: ramas, revisión de código y cambios integrados,
    con mentores al lado todo el tiempo. Si el acceso está preparado, el cambio
    también se cierra con un pull request.
  </p>

  <ol class="roles not-content">
    {ROLES.map((role, index) => (
      <li class="role">
        <span class="role__n" aria-hidden="true">{String(index + 1).padStart(2, "0")}</span>
        <span class="role__name">{role.label}</span>
        <span class="role__hint">{role.hint}</span>
      </li>
    ))}
  </ol>

  <Note kind="tip" title="El entorno ya está listo">
    El repositorio, la arquitectura base y las tareas están preparados de
    antemano. Tu tiempo se va en construir, no en configurar.
  </Note>
</section>

<section class="section" data-reveal>
  <p class="section__kicker">04 · Al terminar</p>
  <h2 class="section__title">Qué te llevas a casa</h2>
  <p class="section__lead">
    Todo lo de esta lista es tuyo y lo puedes enseñar en una entrevista.
  </p>

  <ul class="deliverables not-content">
    {EVENT.deliverables.map((item) => <li>{item}</li>)}
  </ul>
</section>

<section class="section" data-reveal>
  <div class="cta not-content">
    <div class="cta__copy">
      <p class="section__kicker">05 · Antes de venir</p>
      <h2 class="section__title">Solo necesitas tres cosas</h2>
      <ul class="cta__list">
        {EVENT.bring.map((item) => <li>{item}</li>)}
      </ul>
      <p class="cta__note">
        No necesitas ser experta. Necesitas saber programar algo y querer
        aprender el resto en el proceso: los mentores están ahí precisamente
        para eso.
      </p>
    </div>

    <div class="cta__actions">
      <a class="btn btn--primary" href="/empezar/requisitos">
        Preparar mi entorno
        <span class="btn__arrow" aria-hidden="true">→</span>
      </a>
      <a class="btn btn--ghost" href={EVENT.registrationUrl}>
        Reservar mi lugar
      </a>
    </div>
  </div>
</section>

<Reveal />
